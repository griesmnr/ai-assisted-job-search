node_modules
dist
build
.pnpm-store
pnpm-lock.yaml
*.tsbuildinfo
.rtk
.worktrees

# Hand-maintained PM/setup docs at the repo root, not scaffold-owned
/CLAUDE.md
/SETUP_HISTORY.md

# Drizzle-generated migration snapshots — regenerated by drizzle-kit, which doesn't
# format to prettier standards. No point reformatting; they'll be unformatted again
# on the next migration generation.
apps/api/drizzle/meta/

# Raw API responses captured verbatim from live job boards. Reformatting them
# would make them stop being verbatim captures, which is the entire point —
# a fixture that has been prettified is no longer evidence of what the API
# actually returned.
apps/api/src/sources/__fixtures__/
