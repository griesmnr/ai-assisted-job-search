prep/
.worktrees/

# Secrets — never commit
.env
.env.local
*.env
!.env.example

# Node / pnpm workspace
node_modules/
dist/
build/
dist-node/
*.tsbuildinfo
coverage/

# Tooling
.rtk/
.pnpm-store/

# Vite
apps/*/.vite/

# Local snapshots — never commit (contains secrets)
backups/

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json*
